description: Bad request error
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - data
      properties:
        status:
          type: integer
          example: 400
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: 'Bad request'
